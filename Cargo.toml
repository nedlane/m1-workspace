[package]
name = "m1-workspace"
version = "0.12.0"
edition = "2024"
rust-version = "1.88"
license = "GPL-3.0-or-later"
description = "Shared filesystem and path conventions for the MoTeC M1 toolchain"
repository = "https://github.com/nedlane/m1-workspace"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_ignored = "0.1"
toml = "1.1"

[dev-dependencies]
tempfile = "3"
